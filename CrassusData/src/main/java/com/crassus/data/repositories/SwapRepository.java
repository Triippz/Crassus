package com.crassus.data.repositories;

import com.crassus.models.models.Swap;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SwapRepository extends JpaRepository<Swap, String> {}
